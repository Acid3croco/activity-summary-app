const IntroductionMessage = () => {
  return (
    <p className="text-lg">
      Welcome! This webapp allows you to upload a GPX file from Strava and get a
      summary of your activity, including distance, duration, elevation, and
      pace. To get started, simply upload a GPX file using the button below.
    </p>
  )
}

export default IntroductionMessage
